# Garden Battle Frontend

Angular + Ionic Telegram Mini App shell.

## Run

```powershell
npm install
npm run start
```

The app expects the backend gateway/API under the same origin for `/api/*` and `/battleHub`.
